package com.example.monthlyexpensesapp.bill;

import com.example.monthlyexpensesapp.bill.dto.BillDto;

class BillFactory {

  /*  public static Bill from(BillDto source) {
        var bill = new Bill();
        bill.setClosed(source.isClosed());
        bill.setAccount(source.getAccount());
        bill.setGroup_date(source.getGroup_date());
        bill.setProducts(source.getProducts());
        bill.setShop(source.getShop());
        return bill;
    }*/

}
